#include <iostream>
#include <cstdio>


using namespace std;
int main() {
    int col, row;
    scanf("%d %d", &col, &row);
    while (col != 0 && row != 0){
        char mine[row][col];


        scanf("%d %d", &col, &row);
    }


    return 0;
}